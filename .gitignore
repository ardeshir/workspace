# Downloaded and generated files -- everyone builds their own, none belong in git.
*.iso
*.img
*.qcow2
*.part
SHA256SUMS*

# Your own SSH key must never end up here.
id_*
*.pem
*.key

# Internal admin notes -- this repo is PUBLIC. ADMIN.md holds tenant,
# subscription and app registration identifiers and must never be committed.
ADMIN.md

.DS_Store
